name: Probe Gonggonggangsa public recruitment structure

on:
  workflow_dispatch:
permissions:
  contents: write

concurrency:
  group: probe-gonggonggangsa
  cancel-in-progress: false

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install probe dependencies
        run: |
          python -m pip install --upgrade pip
          pip install playwright
          python -m playwright install --with-deps chromium
      - name: Probe public recruitment structure
        run: python scripts/probe_gonggonggangsa_browser.py
      - name: Commit isolated probe report
        if: always()
        run: |
          if [ ! -f gonggonggangsa_probe_report.json ]; then
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add gonggonggangsa_probe_report.json
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m 'Record isolated Gonggonggangsa structure probe'
          git pull --rebase origin main
          git push origin HEAD:main
