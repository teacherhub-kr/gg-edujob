name: Probe ArtMore visible region controls

on:
  workflow_dispatch:
permissions:
  contents: write

concurrency:
  group: metro-edujob-artmore-visible-region-controls
  cancel-in-progress: true

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Sync latest main
        run: |
          git fetch origin main
          git reset --hard origin/main
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install browser probe dependencies
        run: |
          python -m pip install --disable-pip-version-check playwright
          python -m playwright install --with-deps chromium
      - name: Syntax check
        run: python -m py_compile scripts/probe_artmore_visible_region_controls.py
      - name: Inspect visible region controls
        id: probe
        continue-on-error: true
        run: python scripts/probe_artmore_visible_region_controls.py
      - name: Validate isolation gate
        if: always()
        run: |
          python - <<'PY'
          import json, pathlib
          p=pathlib.Path('artmore_visible_region_controls.json')
          assert p.exists(), 'visible region report missing'
          d=json.loads(p.read_text(encoding='utf-8'))
          assert d.get('publicationEnabled') is False
          assert d.get('mode') == 'read-only-visible-region-control-probe-v1'
          print(json.dumps(d.get('summary',{}),ensure_ascii=False,indent=2))
          PY
      - name: Commit diagnostic evidence
        if: always()
        run: |
          test -f artmore_visible_region_controls.json || exit 0
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add artmore_visible_region_controls.json
          git diff --cached --quiet && exit 0
          git commit -m 'Record ArtMore visible region controls'
          for i in 1 2 3; do
            git pull --rebase origin main && git push origin HEAD:main && break
            sleep $((i*4))
          done
      - name: Fail visibly only when probe execution failed
        if: steps.probe.outcome != 'success'
        run: exit 1
