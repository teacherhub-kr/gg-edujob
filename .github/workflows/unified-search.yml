name: Unified recruitment search

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'scripts/build_unified_search.py'
      - 'scripts/build_unified_search_multi.py'
      - 'scripts/validate_unified_search.py'
      - 'scripts/validate_unified_search_multi.py'
      - 'scripts/validate_required_metro_publication.py'
      - 'scripts/crosscheck_private_official.py'
      - 'scripts/crawl_jobteacher_browser.py'
      - 'scripts/crawl_artmore_browser.py'
      - 'scripts/crawl_artmore_browser_v2.py'
      - 'scripts/crawl_gonggonggangsa_jobs.py'
      - 'scripts/run_lessoninfo_browser_fast.py'
      - 'scripts/run_lessoninfo_browser_resilient.py'
      - '.github/workflows/update-jobteacher-jobs.yml'
      - '.github/workflows/update-artmore-candidate.yml'
      - '.github/workflows/update-lessoninfo-jobs.yml'
      - 'scripts/reconcile_cultural_foundation_coverage.py'
      - 'scripts/source_registry.py'
      - 'scripts/private_source_registry.py'
      - 'scripts/direct_post_links.py'
      - 'scripts/audit_direct_post_links.py'
      - 'scripts/apply_unified_frontend.py'
      - 'scripts/crawl_seekle_instructor_jobs.py'
      - 'scripts/crawl_boramyc_instructor_jobs.py'
      - 'scripts/publication_transaction.py'
      - 'tests/test_boramyc_historical_fixture.py'
      - 'tests/fixtures/boramyc_25441.html'
      - 'tests/test_seekle_historical_fixture.py'
      - 'tests/fixtures/seekle_21827.html'
      - 'verified_foundation_official_sources.json'
      - 'official_foundation_jobs.json'
      - 'official_foundation_report.json'
      - 'cleaneye_foundation_jobs.json'
      - 'cleaneye_foundation_report.json'
      - 'seekle_jobs.json'
      - 'seekle_reconciliation_report.json'
      - 'boramyc_jobs.json'
      - 'boramyc_reconciliation_report.json'
      - 'unified-ui.js'
      - 'metro-ui.js'
      - 'mobile-ui.js'
      - 'direct-link-guard.js'
      - '.github/workflows/unified-search.yml'
permissions:
  contents: write

concurrency:
  group: metro-edujob-data-${{ github.event_name == 'pull_request' && github.event.pull_request.number || github.ref }}
  cancel-in-progress: false

jobs:
  build:
    if: github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Synchronize latest canonical datasets for production runs
        if: github.event_name == 'schedule' || github.event_name == 'workflow_run' || (github.event_name == 'push' && github.ref == 'refs/heads/main') || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')
        run: |
          git fetch origin main
          git checkout -B main origin/main
      - uses: actions/setup-python@v7
        with:
          python-version: '3.12'
      - name: Capture immutable publication baseline
        if: github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'
        run: python scripts/publication_transaction.py capture --output /tmp/unified_publication_base_sha
      - name: Install dependencies
        run: python -m pip install --disable-pip-version-check -r requirements.txt
      - name: Syntax check unified multi-source pipeline and UI
        run: |
          python -m py_compile scripts/source_registry.py scripts/private_source_registry.py scripts/build_unified_search.py scripts/build_unified_search_multi.py scripts/validate_unified_search_multi.py scripts/validate_required_metro_publication.py scripts/crosscheck_private_official.py scripts/reconcile_cultural_foundation_coverage.py scripts/direct_post_links.py scripts/audit_direct_post_links.py scripts/apply_unified_frontend.py scripts/crawl_seekle_instructor_jobs.py scripts/crawl_boramyc_instructor_jobs.py scripts/crawl_jobteacher_browser.py scripts/crawl_artmore_browser.py scripts/crawl_artmore_browser_v2.py scripts/crawl_gonggonggangsa_jobs.py scripts/run_lessoninfo_browser_fast.py scripts/run_lessoninfo_browser_resilient.py
          node --check unified-ui.js
          node --check metro-ui.js
          node --check mobile-ui.js
          node --check direct-link-guard.js
      - name: Verify source registries resolve independently
        run: |
          python - <<'PY'
          import sys
          sys.path.insert(0,'scripts')
          from source_registry import official_source_count
          from private_source_registry import private_source_keys
          official=official_source_count(); private=private_source_keys()
          assert official > 0
          assert len(private) == len(set(private))
          assert set(private) >= {'lessoninfo','cleaneye','jobteacher','artmore','gonggonggangsa','seekle','boramyc'}
          print('registry check', official, private)
          PY
      - name: Historical private-source parser and direct-link regressions
        run: python -m unittest tests.test_boramyc_historical_fixture tests.test_seekle_historical_fixture
      - name: Build disposable multi-source unified candidate
        id: build_candidate
        continue-on-error: true
        run: python scripts/build_unified_search_multi.py
      - name: Repair stable-ID links and audit exact posting destinations
        id: link_audit
        if: steps.build_candidate.outcome == 'success'
        continue-on-error: true
        run: python scripts/audit_direct_post_links.py --input unified_jobs.next.json --repair-in-place --strict-clickable
      - name: Validate candidate against every publication-enabled canonical source
        id: validate_candidate
        if: steps.build_candidate.outcome == 'success' && steps.link_audit.outcome == 'success'
        continue-on-error: true
        run: python scripts/validate_unified_search_multi.py
      - name: Crosscheck private school signals against official data
        id: crosscheck
        if: steps.validate_candidate.outcome == 'success'
        continue-on-error: true
        run: python scripts/crosscheck_private_official.py
      - name: Publish only verified unified dataset without mutating frontend
        id: prepare_publication
        if: steps.build_candidate.outcome == 'success' && steps.link_audit.outcome == 'success' && steps.validate_candidate.outcome == 'success'
        run: |
          mv unified_jobs.next.json unified_jobs.json
          python scripts/validate_required_metro_publication.py
          grep -Fq "fetch('unified_jobs.json',{cache:'no-cache'})" app.js
          grep -Fq "const provinceOptions=()=>['경기','서울','인천']" app.js
          grep -Fq '<button type="button" data-surface="학원·민간">학원·민간</button>' index.html
          grep -Fq 'app.js?v=' index.html
          python - <<'PY'
          import json,sys
          sys.path.insert(0,'scripts')
          from private_source_registry import private_source_keys
          from source_registry import official_source_count
          from direct_post_links import is_direct_post
          d=json.load(open('unified_jobs.json',encoding='utf-8'))
          assert d.get('dataset') == 'unified-search-v3-multi-private'
          private=d.get('privateSources',{}); expected=set(private_source_keys())
          assert set(private) == expected, (set(private), expected)
          official_count=official_source_count(); assert int(d.get('officialSourceCount') or 0) == official_count
          enabled={k for k,v in private.items() if (v or {}).get('publicationEnabled') is True}
          assert d.get('totalSourceCount') == official_count + len(enabled), (d.get('totalSourceCount'), official_count, enabled)
          assert all((private[k] or {}).get('ok') for k in enabled)
          assert all(int((private[k] or {}).get('count') or 0) == 0 for k in expected-enabled)
          assert any(j.get('feedKind')=='official' for j in d['jobs'])
          assert any(j.get('feedKind')=='private' for j in d['jobs'])
          for key in enabled:
              source_name=(private[key] or {}).get('name')
              assert any(j.get('source')==source_name for j in d['jobs']) or int((private[key] or {}).get('count') or 0) == 0
          disabled_names={(private[k] or {}).get('name') for k in expected-enabled}
          leaked=[j for j in d['jobs'] if j.get('feedKind')=='private' and j.get('source') in disabled_names]
          assert not leaked, f'publication-disabled private rows: {len(leaked)}'
          allowed={'서울','경기','인천'}
          def provinces(j):
              ps={str(x) for x in (j.get('provinces') or []) if str(x)}
              if ps: return ps
              p=str(j.get('province') or '')
              return {p} if p else set()
          bad=[j for j in d['jobs'] if j.get('feedKind')=='private' and (not provinces(j) or not provinces(j).issubset(allowed))]
          assert not bad, f'non-metro private rows: {len(bad)}'
          unsafe=[j for j in d['jobs'] if j.get('detailLinkVerified') is not False and not is_direct_post(j)[0]]
          assert not unsafe, f'unsafe clickable posting links: {len(unsafe)}'
          foundation_official=[j for j in d['jobs'] if j.get('sourceRole')=='primary-official' and j.get('sourceSurface')=='cultural-foundation']
          assert all(j.get('foundationRegistryId') and j.get('trustLevel')=='공식' and is_direct_post(j)[0] for j in foundation_official)
          cleaneye=[j for j in d['jobs'] if j.get('source')=='클린아이 잡플러스']
          for j in cleaneye:
              assert j.get('trustLevel')=='공공' and j.get('sourceRole')=='public-foundation-safety-source'
              assert j.get('foundationRegistryId') and is_direct_post(j)[0]
          lesson_culture=[j for j in d['jobs'] if j.get('source')=='레슨인포' and j.get('sourceSurface')=='culture-arts']
          assert all(j.get('sourceRole')=='discovery-only' for j in lesson_culture)
          print('verified publication-gated multi-source unified publication',len(d['jobs']),official_count,sorted(enabled),len(foundation_official))
          PY
      - name: Reconcile cultural foundation signals against published search
        id: foundation_reconcile
        if: steps.build_candidate.outcome == 'success' && steps.link_audit.outcome == 'success' && steps.validate_candidate.outcome == 'success'
        continue-on-error: true
        run: python scripts/reconcile_cultural_foundation_coverage.py
      - name: Publish verified search transaction
        if: github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main' && steps.prepare_publication.outcome == 'success' && steps.foundation_reconcile.outcome == 'success' && steps.crosscheck.outcome == 'success'
        shell: bash
        run: |
          set -euo pipefail
          rm -f unified_jobs.next.json
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add unified_search_report.json unified_validation_report.json unified_crosscheck_report.json direct_post_link_report.json cultural_foundation_coverage_report.json 2>/dev/null || true
          git add unified_jobs.json
          git diff --cached --quiet && exit 0
          python scripts/publication_transaction.py publish \
            --baseline-file /tmp/unified_publication_base_sha \
            --status-file /tmp/unified-publication-transaction.json \
            --message 'Publish verified multi-source recruitment search'
      - name: Upload unified publication transaction status
        if: always() && github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'
        uses: actions/upload-artifact@v4
        with:
          name: unified-publication-${{ github.run_id }}-${{ github.run_attempt }}
          path: /tmp/unified-publication-transaction.json
          if-no-files-found: warn
          retention-days: 14
      - name: Clean branch/PR test artifacts
        if: always() && !(github.event_name == 'schedule' || github.event_name == 'workflow_run' || (github.event_name == 'push' && github.ref == 'refs/heads/main'))
        run: rm -f unified_jobs.next.json
      - name: Fail after preserving diagnostics
        if: steps.build_candidate.outcome == 'failure' || steps.link_audit.outcome == 'failure' || steps.validate_candidate.outcome != 'success' || steps.crosscheck.outcome == 'failure' || steps.foundation_reconcile.outcome == 'failure'
        run: exit 1
